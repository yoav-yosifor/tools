# Laser Sweep Module Requirements
# Install with: pip install -r requirements.txt

# Core scientific computing
numpy>=1.20.0
pandas>=1.3.0

# Visualization
matplotlib>=3.3.0

# Instrument control - VISA
pyvisa>=1.11.0
pyvisa-py>=0.5.0

# PicoScope support
picosdk>=1.0.0

# Optional: For better VISA backend support
# Uncomment if you want to use PyVISA-py as backend (no NI-VISA needed)
# pyvisa-py>=0.5.0

# Optional: For GPIB support via linux-gpib
# Uncomment if using linux-gpib
# linux-gpib>=4.3.0

# Type hints (included in Python 3.5+, but listing for completeness)
# typing>=3.7.4
